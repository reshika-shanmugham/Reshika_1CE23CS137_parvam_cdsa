#include<stdio.h>
int main(){
    int a=2;
    int b=4;
    int add=a+b;
    int sub=a-b;
    int mul=a*b;
    int div=a/b;
    int mod=a%b;
    printf("The sum, difference, product,quotient and remainder of the two numbers %d and %d are:%d,%d,%d,%d,%d respectively",a,b,add,sub,mul,div,mod);
}